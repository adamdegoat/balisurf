# Cloudflare Pages settings for sumbasurf.pages.dev: a little storage for the player alerts (it remembers which
# Telegram chat to message). The bot key itself is a secret set in Cloudflare, not here.
name = "sumbasurf"
pages_build_output_dir = "."
compatibility_date = "2026-09-01"

[[kv_namespaces]]
binding = "KV"
id = "a652d727290b45eda5efcca6703606ec"
